const radius = 6
const Circle_area = Math.PI * radius ** 2
console.log (Circle_area.toFixed(2))

const length = 12
const width = 6
const Rectangle_area = length * width
console.log (Rectangle_area.toFixed(2))

const radius2 = 7
const height = 4
const Cylinder_volume = Math.PI * radius2 ** 2 * height
console.log (Cylinder_volume.toFixed(2))